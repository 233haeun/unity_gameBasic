✅ 공동작업자 → 맨 먼저 할 일 : git pull로 최신 정보를 받아온다

5월 19일에 작업한 UI 와 21일에 작업한 start Scene 이미지를 내려받는다

(할 일 작업)

✅ 다른 기능 및 작업을 위해서 지역저장소에 새로운 브랜치를 만들고 새로운 브랜치로 이동(checkout)

`git checkout -b <branch 이름>`

이 과정에서 브랜치명에 대한 브랜치가 이미 있다면 해당 브랜치로 이동(checkout)

→ `git checkout <branch 이름>`

✅ 브랜치를 이동했다면 작업 후 commit 

`git add <파일>
 git commit -m "변경사항"`

✅ 원격 저장소에 push
 git push 명령 뒤에 origin <branch 이름> 을 적어 주면 원격저장소(origin)에 새로운 branch를 push한다

`git push origin <branch 이름>`

---

**Branch 명령어 정리**

✅ branch 생성

`git branch <branch 이름>` 

✅ branch 생성 및 생성된 브랜치로 이동

`git checkout -b <branch 이름>` 

✅ 연결된 branch 확인

`git branch`

✅ branch로 이동 (branch 변경)

`git checkout <branch 이름>` 

✅ branch 삭제

`git branch -d <branch 이름>`

✅ branch 강제삭제 (-d 옵션으로 삭제가 안될 경우)

`git branch -D <branch 이름>` 

✅ branch에 push

`git push origin <branch 이름>` 

branch 이름이 아닌 main을 쓰면 main에 push 되므로 주의할 것 (main에 변경사항 반영됨)

`git push origin main`
